name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22  # ← mudamos para 22

      - name: Install dependencies
        run: npm install

      - name: Install Capacitor globally
        run: npm install -g @capacitor/cli

      - name: Install Capacitor core and android
        run: npm install @capacitor/core @capacitor/android

      - name: Init Capacitor
        run: npx cap init RodaDoSabor com.siddapichau.rodasabor --web-dir .

      - name: Add Android
        run: npx cap add android

      - name: Sync Capacitor
        run: npx cap sync

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
